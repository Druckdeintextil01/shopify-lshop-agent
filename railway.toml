[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npm install && npx playwright install chromium && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
